// No need for this since we are no longer using Ajax for emptying cart
// $('#cart').html("<%= j render :partial => 'carts/cart' %>").hide('blind', 50);


// The following are all different code that do as the above separated by two new lines


// $('#cart tr').not('.total_line').remove();
// $('#cart').hide('blind', 50);
//
//
// if ($('#cart').length == 1) { $('#cart').hide("blind", {direction: "vertical" }, 1000)};
//
//
// $('#cart tr').not('.total_line').remove();
